<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Bready — Freshly Baked</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/static/mycss.css" />
  </head>
  <body class="min-h-screen bg-amber-50 text-amber-900 antialiased">
    {% include "components/navbar.html.jinja" %}

    <header class=" bg-gradient-to-r from-orange-200 via-orange-100 to-orange-50">
      <div class="max-w-7xl mx-auto px-6 py-20 lg:py-28">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
          <div>
            <h1 class="text-4xl sm:text-5xl font-extrabold leading-tight">Bready, Freshly, and Steady baked every day</h1>
            <p class="mt-4 text-lg text-amber-700">Be bready to have a nice visit throughout this whole store of things made from WHEAT!.</p>
            <div class="mt-6 flex flex-wrap gap-3">
              <a href="/browse" class="inline-block bg-amber-700 text-white px-6 py-3 rounded-lg shadow hover:bg-amber-800">Browse Breads</a>
              <a href="/login" class="inline-block border border-amber-700 text-amber-700 px-6 py-3 rounded-lg hover:bg-amber-100">Sign up now!</a>
            </div>
            <ul class="mt-6 flex flex-wrap gap-4 text-sm text-amber-700">
              <li class="flex items-center gap-2">🥖 Baguettes</li>
              <li class="flex items-center gap-2">🥐 Croissants</li>
              <li class="flex items-center gap-2">🍞 And more!</li>
            </ul>
          </div>

          <div class="flex justify-center lg:justify-end">
            <div class="w-80 h-56 bg-white rounded-2xl shadow-lg overflow-hidden border border-amber-100">
              <img alt="Whole wheat loaf" src="/static/products/sbagel.webp" class="w-full h-full object-cover"/>
            </div>
          </div>
        </div>
      </div>
    


    <section class="max-w-7xl mx-auto px-6 py-8">
      <div class="flex items-center justify-between">
        <h2 class="text-2xl font-bold">Featured Breads</h2>
        <a href="/browse" class="text-amber-700 hover:underline">See all</a>
      </div>

      <div class="mt-6 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {% for product in products[:3] %}
        <article class="bg-white rounded-lg shadow overflow-hidden">
          <img src="{{ product['Image'] }}" alt="{{ product['Name'] }}" class="w-full h-44 object-cover"/>
          <div class="p-4">
            <h3 class="font-semibold">{{ product['Name'] }}</h3>
            <div class="mt-2 flex items-center justify-between">
              <div class="text-amber-700 font-bold">${{ product['Price'] }}</div>
              <a href="/product/{{ product['Name'] | replace(' ','-') | lower }}" class="text-sm bg-amber-700 text-white px-3 py-1 rounded">View</a>
            </div>
          </div>
        </article>
        {% endfor %}
      </div>
    </section>
    </header>

  </body>
</html>