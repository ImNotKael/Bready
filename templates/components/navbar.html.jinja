<nav class="bg-amber-900 p-4 flex items-center justify-between border-1 border-black">
    <a class="text-2xl text-white font-bold ml-4" href="/">Bready Store</a>


    <div class="flex gap-5 items-center text-white">
            <a href="/contact">Contact us!</a>
            <a href="/browse">Browse</a>
            <a href="/product/1">Products</a>
            <a href="/cart">Cart</a>
            {%if not current_user.is_authenticated %}
            <a href="/login">Log in!</a>
            <a href="/register">Sign up!</a>
            {%else%}
                <div class="bg-orange-700 p-1">
                    Hi, {{ current_user.name }}!
                </div>
            {%endif%}
    </div>
</nav>